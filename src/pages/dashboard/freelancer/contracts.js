import { DashboardPageHeader } from '@/shared/components/DashboardPageHeader';

function Contracts() {
  return (
    <DashboardPageHeader
      title="Contracts"
      descr={`All the contracts you recieve from clients get displayed here, 
        view and manage all your contracts`}
    />
  );
}

export default Contracts;

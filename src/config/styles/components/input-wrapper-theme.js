export const InputWrapperTheme = {
  styles: (theme) => ({
    label: theme.other.font.label.size[1],
    description: theme.other.font.label.size[1],
    error: theme.other.font.label.size[1],
  }),
};
